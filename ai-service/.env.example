# Optional. If set, quiz generation uses Gemini for richer conceptual
# questions. If unset, falls back to the offline rule-based generator.
GEMINI_API_KEY=

# Origin allowed to call this service (the Node backend)
BACKEND_URL=http://localhost:5000
